{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Release.Name }}-gw
{{- if or (gt (len .Values.gateway.annotations) 0) .Values.certmanager.enabled }}
  annotations:
    {{- with .Values.gateway.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.certmanager.enabled }}
    cert-manager.io/issuer:          "letsencrypt-{{ .Release.Name }}"
    cert-manager.io/issue-temporary-certificate: "true"
    acme.cert-manager.io/http01-edit-in-place: "true"
    {{- end }}
{{- end }}
spec:
  gatewayClassName: {{ required "gateway.className is required" .Values.gateway.className }}
  infrastructure:
    {{- if gt (len .Values.gateway.infrastructureAnnotations) 0 }}
    annotations: {{ toJson .Values.gateway.infrastructureAnnotations }}
    {{- end }}  
  listeners:
  {{- if eq (len .Values.gateway.hostNames) 0 }}
    {{- fail "Minimum one entry is required in gateway.hostNames" }}
  {{- end }}
  {{ $releaseName := .Release.Name }}
  {{- range $i, $host := .Values.gateway.hostNames }}
  {{- if $.Values.certmanager.enabled }}
  # Cert Manager Listener
    - name: public-http-{{ $host }}
      protocol: HTTP
      port: 80 # This should be always port 80 as per https://cert-manager.io/docs/configuration/acme/http01/#configuring-the-http-01-gateway-api-solver
      allowedRoutes:
        namespaces:
          from: Same
  {{- end }}
    - name: websecure-{{ $host }}
      hostname: {{ $host }}
      port: {{ required "gateway.listenerPortSecure is required" $.Values.gateway.listenerPortSecure }}
      protocol: HTTPS
      allowedRoutes:
        namespaces:
          from: Same
      {{- if $.Values.tls.enabled }}
      tls:
        mode: Terminate
        certificateRefs:
          - name: {{ $.Values.tls.certRef }}
      {{- end }}
  {{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Release.Name }}-api-route
spec:
  hostnames:
    {{- range .Values.snapagogo.api.hostNames }}
    - {{ . }}
    {{- end }}
  parentRefs:
  {{- range $i, $host := .Values.snapagogo.api.hostNames }}
    - group: gateway.networking.k8s.io
      sectionName: websecure-{{ $host }}
      kind: Gateway
      name: {{ $.Release.Name }}-gw
  {{- end }}
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: {{ .Release.Name }}-api-service
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /auth
      {{- if .Values.gateway.requestTimeout }}
      timeouts:
        request: "{{ .Values.gateway.requestTimeout }}"
        backendRequest: "{{ .Values.gateway.requestTimeout }}"
      {{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Release.Name }}-ui-route
spec:
  hostnames:
    {{- range .Values.snapagogo.ui.hostNames }}
    - {{ . }}
    {{- end }}
  parentRefs:
  {{- range $i, $host := .Values.snapagogo.ui.hostNames }}
    - group: gateway.networking.k8s.io
      sectionName: websecure-{{ $host }}
      kind: Gateway
      name: {{ $.Release.Name }}-gw
  {{- end }}
  rules:
    - backendRefs:
        - group: ''
          kind: Service
          name: {{ .Release.Name }}-ui-service
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /auth
      {{- if .Values.gateway.requestTimeout }}
      timeouts:
        request: "{{ .Values.gateway.requestTimeout }}"
        backendRequest: "{{ .Values.gateway.requestTimeout }}"
      {{- end }}

{{- if .Values.certmanager.enabled }}
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: letsencrypt-{{ .Release.Name }}
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-key
    email: {{ required "certmanager.email required" .Values.certmanager.email }}
    solvers:
      - http01:
          gatewayHTTPRoute:
            parentRefs:
              - name: {{ .Release.Name }}-gw
                namespace: {{ .Release.Namespace }}
                kind: Gateway
{{- end }}
{{- end }}